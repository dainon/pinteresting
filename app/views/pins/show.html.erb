<%= image_tag @pin.image.url%>

<p>
  <strong>Description:</strong>
  <%= @pin.description %>
</p>

<% if @pin.user == user_signed_in? %>
	<%= link_to 'Edit', edit_pin_path(@pin) %> |
<% end %>
<%= link_to 'Back', pins_path %>
